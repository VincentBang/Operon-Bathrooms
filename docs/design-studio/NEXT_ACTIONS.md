# Design Studio Next Actions

1. Review and approve the Phase 6 handoff-contract closeout PR.
2. After closeout merge, keep Phase 7 locked until Vincent explicitly approves the next phase.
3. Keep full Quote OS, pricing, proposals, procurement, public Quote OS output, payment, CRM, supplier/SKU data, storage, Supabase migrations and admin automation locked.
4. Use `docs/design-studio/INTERNAL_PILOT_OPERATIONS_CHECKLIST.md` for repeatable internal-only `/design-studio` runs.
5. Keep human feedback collection optional for internal-only continuation, but required again before any external exposure decision unless separately waived.
6. Monitor the mobile chatbot launcher overlay near lower-right content; open a polish branch only if it blocks completion or critical controls.
7. Do not set discovery to `public` without a separate approval.
8. Keep user-entered approximate measurements, AR/browser-camera experiments, public indexing, external exposure and broad release changes locked until separate approval.
