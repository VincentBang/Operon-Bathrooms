# Design Studio Next Actions

1. Decide whether to run a true human VoiceOver or equivalent screen-reader pass before external pilot exposure, or explicitly accept the proxy evidence for an internal-only pilot while discovery stays hidden.
2. Keep `NEXT_PUBLIC_ENABLE_BATHROOM_DESIGN_STUDIO=true` and `NEXT_PUBLIC_BATHROOM_DESIGN_STUDIO_DISCOVERY=` as the controlled noindex pilot configuration; do not set discovery to `public` without a separate approval.
3. Do not add camera, upload, storage, LiDAR, BIM, production AR, measured-plan, pricing, procurement, Quote OS, payment, CRM or marketplace work without a specific gate approval.
4. Keep user-entered approximate measurements, AR/browser-camera experiments, public indexing and broad release changes locked until separate approval.
