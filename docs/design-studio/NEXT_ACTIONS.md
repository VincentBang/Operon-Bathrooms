# Design Studio Next Actions

1. Review the screen-reader proxy pass audit branch.
2. Approve merge, request changes or pause controlled noindex pilot work.
3. Complete a true human VoiceOver or equivalent screen-reader pass before external pilot exposure, unless Vincent explicitly accepts proxy evidence for an internal-only pilot while discovery stays hidden.
4. Keep `NEXT_PUBLIC_ENABLE_BATHROOM_DESIGN_STUDIO=true` and `NEXT_PUBLIC_BATHROOM_DESIGN_STUDIO_DISCOVERY=` as the controlled noindex pilot configuration; do not set discovery to `public` without a separate approval.
5. Do not add camera, upload, storage, LiDAR, BIM, production AR, measured-plan, pricing, procurement, Quote OS, payment, CRM or marketplace work without a specific gate approval.
6. Keep user-entered approximate measurements, AR/browser-camera experiments, public indexing and broad release changes locked until separate approval.
